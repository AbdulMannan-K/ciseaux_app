import React from 'react';
import { View, Text } from 'react-native';

const CartPage = () => {
    return (
        <View className='flex-1 justify-center'>
            <Text>Cart Page - Show items in the cart</Text>
        </View>
    );
};

export default CartPage;

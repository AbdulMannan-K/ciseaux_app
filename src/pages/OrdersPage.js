import React from 'react';
import { View, Text } from 'react-native';

const OrdersPage = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Orders Page - Show past and current orders</Text>
        </View>
    );
};

export default OrdersPage;
